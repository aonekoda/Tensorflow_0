### 05LSTM 텍스트 분류
```
model_lstm.add(Embedding(vocab_size, 10))  # one-hot encoding 대신에 Embedding을 사용
model_lstm.add(LSTM(32, input_shape=(100,100)))
model_lstm.add(Dense(1, activation='sigmoid'))

model_lstm.summary()
```
